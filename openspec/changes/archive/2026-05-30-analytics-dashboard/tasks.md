## 11. Analytics Dashboard

- [x] 11.1 Write failing read-model tests, then implement server-side aggregation queries
  for category and bias frequency scoped to the authenticated user and limited to ready
  analyses only.
- [x] 11.2 Write failing dashboard chart tests, then add Recharts and build token-themed
  category-frequency and bias-frequency charts with localized taxonomy labels.
- [x] 11.3 Write failing empty-state tests, then add the dashboard empty state for users
  with no ready analyses.
- [x] 11.4 Wire the localized dashboard route, navigation affordance, and messages without
  exposing raw decision content in telemetry or chart payloads.
- [x] 11.5 Run `pnpm lint`, `pnpm typecheck`, and `pnpm test`.
