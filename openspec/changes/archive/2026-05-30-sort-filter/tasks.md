## 10. Filtering, Sorting, and Complexity

- [x] 10.1 Write failing read-model/unit tests for derived complexity, then implement
  complexity as the count of biases plus premortem risks plus missed alternatives from the
  newest ready analysis.
- [x] 10.2 Write failing read-model/page-boundary tests for filtering by category and by
  presence of a specific bias, then implement validated filter options using the canonical
  taxonomies and session-scoped decision reads.
- [x] 10.3 Write failing sorting tests for creation time and complexity, then implement
  stable sorting with no-ready decisions ordered last for complexity sorts.
- [x] 10.4 Write failing component/page tests for the controls, then wire localized
  category, bias, and sort controls into the decision history list view with URL query
  state.
- [x] 10.5 Run the relevant quality gates: `pnpm lint`, `pnpm typecheck`, and the targeted
  unit/component tests for decision history filtering, sorting, and controls.
